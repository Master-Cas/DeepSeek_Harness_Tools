/**
 * Compiled bundle fixture: the one-locale-per-call tuple form
 * `register(NS, locale, dict)` over a `[["en", { ... }]]` list, mirroring
 * `ui-directory-picker-browse`.
 */
//#region lib/types/client/index.js
const LOCALE_NS = "compiled.three";
function apply$1(ctx) {
	ctx.effect(() => {
		const dictionaries = [["zh", {
			"bye": "再见"
		}], ["en", {
			"bye": "Bye {who}",
			"ok": "OK"
		}]];
		for (const [locale, dict] of dictionaries)
			ctx.locale.register(LOCALE_NS, locale, dict);
	}, "ui-compiled-three: dictionaries");
}
export { apply$1 as apply };
//#endregion
