/**
 * Compiled bundle fixture under a per-profile store
 * (`profiles/web/node_modules/@deepseek-ai`), mirroring pnpm's `~/.dsh`
 * layout. Exercises spread bases, a `name = { ... }` assignment without
 * `const`, string concatenation and a resolvable template literal.
 */
//#region lib/types/client/locales.js
const NS = "compiled.four";
const BASE = {
	"base": "Base"
};
const NAME = "world";
let en;
en = {
	...BASE,
	"concat": "A" + "B",
	"tpl": `Hi ${NAME}`
};
//#endregion
//#region lib/types/client/index.js
function apply$1(ctx) {
	ctx.effect(() => ctx.locale.register(NS, {
		en
	}), "ui-compiled-four: dictionaries");
}
export { apply$1 as apply };
//#endregion
